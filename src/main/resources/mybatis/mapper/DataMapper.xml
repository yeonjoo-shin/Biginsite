<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bigin.api.advertiser.repository.DataMapper">
    <select id="insertData" parameterType="SDRegisterRequest">
        INTSERT INTO SHOPDATA(
        ADVER_ID,
        PRDT_CODE,
        PRDT_PRICE,
        PRDT_IMG,
        PRDT_URL,
        REGDATE,
        ARTDATE
        )
        VALUES (
        #{adverId},
        #{prdtCode},
        #{prdtPrice},
        #{prdtImg},
        #{prdtUrl},
        now(),
        #{artDate}
        )
    </select>
</mapper>